```
# build the containers
$ docker-compose build

# up creates+starts the images, leaving you with running containers
$ docker-compose up

# start will start stopped containers
$ docker-compose start

# stop will stop, though not remove, the containers
$ docker-compose stop

# down removes the containers
$ docker-compose down
```
